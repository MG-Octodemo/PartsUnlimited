
name: Manage Webhook Event
on:
  workflow_dispatch: 

jobs:
  Check:
    runs-on: ubuntu-latest
    steps:
      - name: Dump GitHub event
        env:
          GITHUB_CONTEXT: ${{ toJson(github.event) }}
        run: echo "$GITHUB_CONTEXT"
